Conway's Game of Life in c printed step by step in the terminal.
At each step, the user can either quit the program by writing an input starting with 'q', or write anything else to show the next step.
The program will interpret its first argument as the filename of the file from which it will get the initial state of the game.
If no argument is given, the program will run with an example initial state of the game.
